glossary = {
    'print': 'outputs to the screen',
    'title()': 'capitalizes the first letter for each word in a string',
    'lower()': 'makes all letters in a string lowercase',
    'upper()': 'makes all letters in a string uppercase',
    'if-elif-else': 'statements to check if something is true, the moves to the elif if not, then else if there is one'}

for key, value in glossary.items():
    print(f"{key}: {value}")